Extends is when a subclass inherits from a superclass and CAN implement the methods differently (overriding).

Implements is when a class fulfills the contract from some interface. The implementing class MUST implement the methods because the interface only gives method signitures.