The two frames are main and then main calls printPerson() which creates a frame above main on the stack.

The actual object is new Person("Aman") and the reference is the variable named person. The reference is like a pointer to the space in memory where the actual object is.
