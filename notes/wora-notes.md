javac compiles the .java file and produces the .class file.
java runs the .class which is already compiled from .java on the jvm
If I changed the .java to say, "Goodbye", it wouldn't change because until I recompile.
The .class file will run the same regardless of OS because it is running in the JVM